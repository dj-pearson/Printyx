# Development-specific environment variables
# Copy relevant values from .env.example and add these development overrides

# Disable subscription jobs in development to reduce database load and errors
DISABLE_SUBSCRIPTION_JOBS=true

# These are already disabled by default in development (documented here for reference)
# ENABLE_DATABASE_UPDATER=false (default in dev)
# ENABLE_WEBSOCKET=false (default in dev)
