# Cloudflare Pages SPA redirects

# NOTE: Cloudflare Pages _redirects proxy doesn't support POST requests to external URLs
# Use VITE_API_BASE_URL environment variable instead and configure CORS on the backend

# All routes should be handled by React Router (wouter)
/* /index.html 200

