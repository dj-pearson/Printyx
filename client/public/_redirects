# Cloudflare Pages SPA redirects

# API proxy - routes /api/* requests to the backend API server
# This avoids CORS issues by keeping everything on the same domain
/api/* https://api.printyx.net/api/:splat 200

# All other routes should be handled by React Router (wouter)
/* /index.html 200

