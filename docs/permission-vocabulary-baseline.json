{
  "note": "SEC-EDGE-002 drove this to 0 and the guard is a HARD GATE - it now refuses to run against a non-empty list. A route gated on a code the RBAC seeder never creates denies every role below platform admin. Fix one by naming a code the seeder creates, or by adding the code to rbac-seeder.ts AND granting it to a role. Do not copy such a gate to an edge function, which would export the lockout to production.",
  "allowed": []
}
