{
  "note": "QUALITY-002 tsc-error ratchet baseline. scripts/check-types.mjs fails CI only when the error count grows above `total`. Lower it (never raise it) as debt is fixed: node scripts/check-types.mjs --update-baseline. 2026-06-26 batch 5: typed EquipmentHealthDashboard.tsx's equipmentHealth useMemo as EquipmentHealth[] (clears 25 implicit-any TS7006 callback params) + added PieChart dataKey/nameKey props (TS2739). Under the script's regex-match count (the authoritative method; --pretty false suppresses tsc's 'Found N' summary): 3912 -> 3886 (-26, no new errors). The prior baseline 3910 was a grep-of-lines undercount of the same HEAD (2 multi-match lines); reconciled here to the script's method.",
  "total": 3886
}
