{
  "note": "QUALITY-002 tsc-error ratchet baseline. scripts/check-types.mjs fails CI only when the error count grows above `total`. Lower it (never raise it) as debt is fixed: node scripts/check-types.mjs --update-baseline. 2026-07-29 (QUALITY-002 batch 27): 1083 -> 1067 — a DEAD-CODE SWEEP, not a type fix. Deleted server/reporting-rbac-middleware.ts, which contributed all 16: it is a never-reachable duplicate of the canonical server/middleware/hierarchical-query-builder.ts and could never have run at all — its HierarchicalQueryBuilder passed STRING column names to Drizzle (eq(`${prefix}tenant_id`, ...)), which is exactly what the 15 TS2769 'No overload matches this call' errors were. Also deleted 5 unreachable client files (0 tsc errors, but -78 ESLint problems): pages/workflow-automation.tsx (a CASE-ONLY duplicate of the routed WorkflowAutomation.tsx), pages/onboarding.tsx, pages/enhanced-crm.tsx, pages/LeadIntelligenceDashboard.tsx, components/layout/CollapsibleSidebar.tsx (duplicate of the live RoleAwareCollapsibleSidebar). Deadness was proven two ways per file — import-graph BFS from ALL 158 entry points (server/index.ts, App.tsx, main.tsx, every npm-script tsx/node target, every test file) plus an exact import-specifier grep — because 'not in App.tsx' is not sufficient.",
  "total": 1067
}
